# Otopsi Raporu ve Borç Defteri

*İlk denetim: 11 Temmuz 2026 · Durum yenileme: 2 Ağustos 2026 · [English](AUTOPSY.md) · Durum kutucukları canlıdır — kapatan, commit'iyle işaretlesin.*

Bu belge, kod tabanının acımasız-dürüst denetiminin kalıcı halidir: ne kırık,
ne çift, ne ölü, felsefe nerede ihlal ediliyor — ve ödeme planı. Amaç suçlamak
değil; **devralan mühendisin ilk gününde tüm cesetlerin yerini bilmesi.**
Yöntem: varsayım yok, her bulgu dosya:satır kanıtlı (11 Tem 2026 itibarıyla).

## Karar özeti

**Refaktör, rewrite DEĞİL.** Panel/Agent/RPC omurgası sağlam; golden path
uçtan uca kanıtlı (tek gerçek varlığımız). Sorunlar mimari değil: dikişsizlik,
çift bilgi, test yokluğu. Rewrite kanıtlanmış davranışı çöpe atar (Netscape hatası).

## A. Kırıklar (bulunduğunda canlıydı)

| # | Bulgu | Kanıt | Durum |
|---|---|---|---|
| A1 | v2 DB handler'ları `TypeID==23/24` bekliyordu; tohum 1=postgresql, 2=mariadb — iki dal da ölü, sürücüye boş tip gidiyordu | `database_v2_handlers.go` (eski 280/428), tohum `001_full_schema.sql:205` | ✅ Kapandı (11 Tem): `GetByID` artık kanonik tip adını JOIN'ler, handler `dbDriverTypeFor()` kullanır |
| A2 | Müşteri/bayi Databases sayfası görüyor (nav ALL) ama `/api/v2/` tümüyle admin kilidinde → admin olmayana her zaman kırık sayfa | `nav.ts:32` ↔ `middleware.go:141` | ✅ KALICI kapandı (17 Tem): battaniye kilit kalktı, uçlar kiracı-kapsamlı (14 denetim), sunucu-kaydı admin'de, nav admin/bayi/müşteri; taze müşteri boş liste görür (404 değil) — canlıda müşteri rolüyle doğrulandı |
| A3 | v2 uçları kiracı-güvenli değil: `subscriptionID := 1 // TODO: Get from auth` | `database_v2_handlers.go:52,106,235,507` | ✅ Kapandı (11 Tem): 6 hardcode kalktı → `callerSubscriptionID`; 6 sunucu-kapsamlı işlem (liste/oluştur/sil × db+kullanıcı) `canAccessDBServer` ile sahiplik doğruluyor (`database_v2_authz.go`). **Admin kilidi HÂLÂ duruyor** — kalkması için `handleCreateDatabaseV2Server`'ın rol ayrımı gerekli (keyfi host/port/root-parola kaydı müşteri işi değil); o v0.3 kiracı işine ait |
| A4 | DB sunucu root parolası düz metin: `// TODO: Encrypt` | `database_v2_handlers.go:138` | ✅ Kapandı (16 Tem): `internal/secrets` — AES-256-GCM, anahtar `dataDir()/secret.key` (0600, ilk açılışta üretilir). Kayıtta mühürle, sürücüde tek yardımcıdan (`dbDriverFor`) çöz; açılışta eski düz-metin satırlar idempotent migrasyonla mühürlenir. Biçim `enc:v1:` önekli — kendini tanımlar |
| A5 | `capabilities.mail_server` BOOL, `dns_server` string — tip tutarsızlığı üründe gerçek hata üretti (pano "posta kuruldu" dedi) | `capabilities_handler.go:30` | ⬜ AÇIK (ön yüz düzeltildi; API tutarlılığı B1'de) |
| A8 | **Statik site PHP kaynağını indirtiyordu**: statik vhost'ta PHP işleyicisi yok — doğru; ama `.php` için ret kuralı da yoktu. nginx dosyayı bulur, uzantıyı tanımaz (`mime.types` içinde yok), `default_type application/octet-stream`e düşer ve tarayıcıya **kaynak kodu indirtir** — içinde veritabanı parolası varsa parola sunucudan çıkar. "Burada PHP çalışmaz", "burada PHP kaynağı yayınlanır" demek olmamalı | `internal/services/templates/nginx/vhost.conf.tmpl` (statik dal) | ✅ Kapandı (21 Tem): `\.(php\|phtml\|phps\|php[0-9])$` reddi; 404 değil 403 bilinçli (dosyanın var olduğunu ama çalıştırılmadığını söyler — asıl teşhis budur). Canlı nginx'te uçtan uca ölçüldü: `secret.php` 403 ve gövde nginx hata sayfası, `.well-known/acme-challenge` 200 (SSL yenileme sağlam), `.env` 403, olmayan dosya 404. 2 regresyon testi. A6 ile aynı aile — web kökündeki dosyanın kaynak olarak dağıtılması |
| A7 | **Tarama önbelleği katalog alanlarını saklıyordu**: `service_scan_cache` tüm API yanıtını (ad, açıklama, ikon, kategori, paket adları) tutuyordu → kodda değişen her katalog alanı, biri "Tara"ya basana dek ekranda eski hâliyle kalıyordu; katalogda YENİ eklenen servis hiç görünmüyordu. Kendi kodundaki gerçeği önbellekten okuyan bir panel | `managed_service_handlers.go` (eski 62-79, 232-243) | ✅ Kapandı (21 Tem): önbellek yalnız `serviceObservation` saklar (kurulu mu / unit ayakta mı / sürümler / config'ler); katalog her okumada `catalogView` ile birleşir — yanıtın kurulduğu TEK yer orası, önbellekli okuma ile taze tarama farklı yanıt veremez. Eski biçim okunmaya devam eder (tarama beklemeden doğru durum). 3 bekçi test. Bu, üründe `kind:""` olarak patladı — aşağıdaki E notu |
| A6 | **vhost nokta-dosya kuralı iki dalda da yanlıştı**: PHP dalı yalnız `/\.ht` engelliyordu → `https://site/.env` düz metin (Laravel/Symfony DB parolası, APP_KEY, posta bilgileri) ve `.git/` açıktı; statik dal ise TÜM nokta dosyalarını engelliyordu → `.well-known/acme-challenge` kapalı, yani statik sitenin ilk sertifikası ve her yenilemesi 403 | `internal/services/templates/nginx/vhost.conf.tmpl` | ✅ Kapandı (20 Tem): iki dal da `location ~ /\.(?!well-known).*`; regex canlı nginx'te doğrulandı (commit `0088c67`). Golden path PHP sitesiyle kanıtlandığı için ACME kırığı görünmez kalmıştı |

| A9 | **PHP sürüm değiştirme iki katmanda kırıktı** (B3d canlı doğrulaması yakaladı, 23 Tem): (1) FPM-root düzeltmesinin kimlik-uydurma kapısı taşımayı da kilitledi — 8.3→8.4 anahtarı 500 veriyordu (iki niyetli iki çağıran tek fonksiyonda); (2) kapı aşılınca ikinci katman göründü: havuz taşınıp DB güncellenirken nginx vhost'u SİLİNEN eski sokete vekillik etmeye devam ediyordu — her şey "başarılı" derken site 502. Config-drift kokusunun (C bölümü) ete kemiğe bürünmüş hâli | `php_pool_manager.go` MigratePool, `domain_php_handlers.go` | ✅ Kapandı (23 Tem): taşıma yeni dosyayı doğrudan yazar (kimlik ESKİ havuzdan — panelin kendi yazdığı; şablon tek üreticide, renderPool), kapı yerinde; PHP değişimi artık `applySiteVhost` ile vhost'u DB satırından yeniden üretir (yaz→doğrula→geri al→yeniden yükle) ve regen başarısızsa istek de başarısızdır. Regresyon testi + canlıda 8.3→8.4→8.3-kaldır zinciri doğrulandı. Ders: **bekçiyi koyan, bekçinin arkasındaki meşru yolları da koşturmalı** — 45c9b2b'nin canlı doğrulaması sürüm değiştirmeyi hiç denememişti |

| A10 | **celikpanel'in nginx entegrasyonu Debian'a özgüydü** (webmail dilimi Arch'ta yakaladı, 24 Tem): site vhost'ları `sites-enabled/`, dbtools + webmail `conf.d/` kullanıyor; Debian nginx bunları otomatik include eder, Arch'ın minimal `nginx.conf`'u HİÇBİRİNİ etmez ve dizinler yok. Sonuç: Arch'ta panelin bıraktığı her vhost/dbtool/webmail conf'u GÖRÜNMEZ — nginx "kurulu" der ama site de dbtool da webmail de sunulamaz. Frankfurt'ta bugüne dek yalnız dnsonly domain vardı, o yüzden site barındırma hiç denenmemiş, boşluk görünmemişti. Operatör webmail üstünden yakaladı ("nginx yokken roundcube nasıl olur, saçmalık") | `nginx_ready.go`, `service_install_handler.go` | ✅ Kapandı (24 Tem): `Agent.EnsureNginxReady` — kurulu nginx'i her dağıtımda panel-hazır yapar (conf.d + sites-available + sites-enabled dizinleri, http bloğuna eksik include'lar, yaz→`nginx -t`→hatada geri al→reload); idempotent, Debian'da dokunmaz. Panel nginx kurulumundan sonra çağırır. İki dağıtımda canlı kanıt: Frankfurt'ta PANELDEN nginx kurunca include'lar eklendi, roundcube /webmail/ 200. **Ders (yeni kural): bir kurulum "başarılı" diyorsa bileşen GERÇEKTEN işlevsel olmalı — "kurulunca çalışır" garanti edilir, yarım kurulum kırıktır.** Ve teşhis dışında hiçbir şey elle SSH'tan yapılmaz; kurulum/kaldırma/yapılandırma panel API'sinden geçer (yoksa panelin kendisi test edilmez) |

| A11 | **Dovecot Arch'ta kurulup çalışmıyordu** (tarayıcı tekleştirmesi görünür kıldı, 24 Tem): (1) Arch'ın hazır `dovecot.conf`'u `/etc/dovecot/ssl-{cert,key}.pem`'e başvuruyor ama Arch onları üretmiyor (Debian'ın `dovecot-core`'u üretir) → daemon "cert_file: open() failed" ile başlamayı reddediyor; (2) `ConfigureMailStack` postfix yoksa toptan reddediyordu, yani tek başına kurulan Dovecot HİÇ yapılandırılmıyordu; (3) agent paketi düşer düşmez unit'i başlatıyor (sertifika henüz yok → failed) ve systemd "failed"de bırakıyor, sonraki doğru yapılandırma bile servisi ayağa kaldırmıyordu | `mail_stack_rpc.go`, `service_install_handler.go`, `main.go` | ✅ Kapandı (24 Tem): `ensureDovecotSSLCert` (yapılandırma o dosyaları istiyorsa ve yoksa openssl ile kendi-imzalı çift; var olana asla dokunmaz — sonradan konan gerçek sertifika sağ çıkar) · yarım yığın meşrudur, hangi yarı varsa o yapılandırılır (yalnız-IMAP geçerli kurulum) · yapılandırmadan SONRA `ResetFailedUnit` + `StartService`. Frankfurt'ta panelden sıfırdan kurulum tek seferde çalıştı (143+993 dinliyor, panel "active (running)"); Boston etkilenmedi. Aynı ders: **panelin kurduğu bileşen gerçekten çalışmalı; dağıtımın eksik bıraktığını panel tamamlar** (D-018 hibrit ilkesi) |

| A12 | **Kurulu spam filtresi hiçbir şeyi süzmüyordu, üstelik Arch'ta Postfix gelen HER postayı reddediyordu** (operatörün sorusu — "spamd ve spamassassin birlikte çalışır mı" — canlı denetime dönüşünce çıktı, 24-25 Tem): (1) Boston'da Rspamd kurulu ve "Çalışıyor"ken `smtpd_milters` BOŞtu — daemon ayakta, portları dinliyor ve içinden tek bir ileti geçmiyor; milter ayarlarının sahibi yoktu, DKIM adımı `smtpd_milters = <opendkim>` yazıyordu ve ikinci bir filtre onu sessizce ezerdi (son yazan kazanır, hata yok). (2) Zincir bağlanıp Frankfurt'a ilk test iletisi gönderilince asıl kırık göründü: `hash:` koda gömülüydü, Arch postfix'i Berkeley DB'siz derliyor → üç sanal harita da ölü, gelen her ileti `451 Temporary lookup failure`. `postconf -m` Arch'ta da `hash` listeler (tip derlenmiş, arka uç yok) — özellik listesi yalancı. (3) Harita tipi düzelince ret bir sonraki eksik dizine taşındı: Debian postinst'inde `newaliases` koşturur, Arch koşturmaz → `aliases.lmdb` yok, her YEREL alıcı da reddediliyor | `mail_stack_rpc.go`, `dkim_signing_rpc.go`, `service_install_handler.go`, `mail_tls_rpc.go` | ✅ Kapandı (25 Tem): `applyMilterChain` milter ayarlarının TEK sahibi (DKIM + spam filtresi birleşir, sıra: DKIM önce/çıkanı imzalar, spam yalnız GELEN postada; `milter_default_action=accept` — ölü filtre posta yutmaz) · kurulum VE kaldırma sonrası zincir yeniden bestelenir, tetikleyici ürün listesi değil KOLTUK (`ConflictGroup=="spam-filter"`) · Rspamd ile SpamAssassin aynı koltukta (teknik olarak yan yana kurulabilirler ama aynı rolü doldururlar: iki kural kümesi, iki kat CPU, çelişen kararlar) · `HelperUnits` katalog alanı — servisin kendisi olmayan köprü daemon'ları (spamd puanlar, postayı uzatan spamass-milter'dır; başlatılmayınca "kurulu, Çalışıyor, süzülen posta sıfır") · `postfixMapType` deneme dosyasında postmap koşturarak lmdb→hash→btree dener, hiçbiri olmazsa texthash · `ensureAliasDatabase` + `postconf -x` (Arch `alias_database = $alias_maps` değerini açılmamış getiriyor) · SNI haritası da tipi keşfediyor, texthash'e düşerse sessiz kalmak yerine hata veriyor · panel açılışında tek seferlik onarım — yükseltmenin kendisi mevcut sunucuları düzeltir. **Uçtan uca canlı kanıt, iki dağıtımda, hepsi PANELDEN kurularak:** GTUBE test iletisi → `554 5.7.1 Gtube pattern`, postfix `milter-reject: END-OF-MESSAGE`, rspamd `Messages scanned 0→1, action reject %100`. Ders: **"kurulunca çalışır" kuralı bileşenin kendisiyle bitmiyor — bileşen yığının İÇİNE bağlı olmalı; ve yol/tip/soket TAHMİN EDİLMEZ, keşfedilir** (A10 nginx include'ları, A11 Dovecot sertifikası ve bu, aynı kökten: dağıtımın paketleme tercihini varsaymak) |

| A13 | **Yönet düğmesi bileşenlerin çoğunda çıkmaz sokaktı; başarısızlıklar hiç kaydedilmiyordu; Netdata Debian'da kurulamıyordu** (operatör, 25 Tem: "birçok servisin manage'i doğru düzgün çalışmıyor. dinamik manage sistemi yok sanırım... net data kurulamadı. ayrıca log yok"): (1) `App.tsx`'te elle yazılmış 9 sayfalık bir switch vardı ama Yönet düğmesi `kind !== 'tool'` olan HER bileşende çiziliyordu — Rspamd, ClamAV, Redis, Memcached, Node tıklanınca "özel sayfa yok" metnine düşüyordu. Elle tutulan ikinci liste kokusunun arayüz sürümü. (2) Denetim kaydı yalnız BAŞARILARI yazıyordu: netdata denemesi hiç iz bırakmamış, başlat/durdur/yeniden başlat ise hiç kaydedilmiyordu — defter kayıt değil vitrindi, operatör ne yaptığını gösteremiyordu. (3) Debian/Ubuntu netdata paketini hiç getirmiyor (`apt-cache policy netdata` → `Candidate: (none)`); katalogda yalnız `pacman` girdisi vardı, yani kurulum apt'ın içinde geç düşüyordu | `App.tsx`, `ComponentDetail.tsx`, `main.go`, `service_install_handler.go`, `managed_services.go` | ✅ Kapandı (25 Tem): `ComponentDetail` — TÜRETİLMİŞ genel sayfa; katalog+gözlemden ne biliniyorsa onu çizer (tür, systemd birimi [id ile aynı olmayabilir], kurulu sürümler, paketler, açık portlar, ayar dosyaları → aynı editör) artı panelden görülemeyen tek şey: `Agent.ServiceJournal` ile kendi günlüğü (süzme+yenile, salt-okunur, unit adı doğrulanır). Özel sayfalar betimlemekten fazlasını yaptıkları için kalır; genel sayfanın hiçbir listeye girmesi gerekmez · başarısız kurulum/kaldırma SEBEBİYLE birlikte ve her başlat/durdur/yeniden başlat denetime yazılır · Netdata için üreticinin resmi deposu (DÜZ/flat biçim: dağıtım adı `{codename}/`, bileşen listesi yok — alışılmış `… trixie main` satırı 404 verirdi) ve yeni `Repo.Required` alanı: depo isteğe bağlı yükseltme değil ZORUNLU adım olarak gösterilir · yanıta `ports` eklendi, portu olmayan bileşen "yalnız yerel" der. **Canlı kanıt:** Frankfurt'ta rspamd günlüğü panelden okundu, kötü unit adı 400 ile reddedildi, üç denetim satırı (başarısız durdur/başarılı yeniden başlat/başarısız kurulum sebebiyle) doğrulandı; Boston'da depo, sistem dosyasına dokunmadan izole apt ile sınandı: `Candidate: (none)` → **2.10.4**. Ders: **her satırda gösterilen bir düğme, her satırda çalışmalı — yoksa elle tutulan liste ekranda yalan söyler**; ve **defter yalnız başarıları yazıyorsa defter değildir** |

| A14 | **WireGuard kurulunca çalışmıyordu: paket iniyor, sunucu yapılandırması hiç oluşmuyordu** (operatör, 25 Tem: "WireGuard VPN server çalışmıyor"): Bileşenler'den kurulum pakette kalıyordu — sunucu anahtarı yok, wg0.conf yok; oynat düğmesi `wg-quick: /etc/wireguard/wg0.conf does not exist` ile düşüyordu. Kurulumu tamamlayan uç (`/api/v1/vpn/setup`) mevcuttu ama yalnız VPN sayfasındaki düğmeye bağlıydı; Bileşenler'den kuran biri onu hiç görmüyordu. A10 (nginx include'ları) ve A11 (Dovecot TLS) ile aynı aile: yarım kurulum | `service_install_handler.go`, `App.tsx` | ✅ Kapandı (25 Tem): kurulum kancası — WireGuard kurulunca panel `Agent.SetupVPN`'i (anahtar + wg0.conf + NAT + IP yönlendirme + etkinleştir/başlat; değişmez etkili, defterdeki istemcileri senkronlar) kendiliğinden çağırır; 51820/udp'yi güvenlik duvarında kurulum akışındaki `syncFirewall` zaten açıyor. WireGuard satırının Yönet'i artık gerçek VPN sayfasına gider. **Canlı kanıt:** Frankfurt'ta Bileşenler'den tek adım kurulum → hemen `configured:true, running:true`, port dinliyor, nft kuralı yerinde; istemci verildi (`wg show` 1 peer). Boston'daki yarım kurulum panel API'sindeki setup ile tamamlandı → `running:true`. Ders aynı: **kurulum, bileşeni yığının içine bağlamadan bitmiş sayılmaz** |

| A15 | **Agent, istekten gelen yola ve URL'e güveniyordu; yapılandırma editörü düzenlediği dosyayi yok edebiliyordu** (uzanti mimarisi ölçülürken çıktı, 25 Tem): (1) `Agent.UpdateConfig`'in tek denetimi "yol `/etc/` ile mi başlıyor" idi, temizleme yok — `/etc/../root/.ssh/authorized_keys` denetimi GEÇİYOR ve /root/.ssh'a düşüyordu; `/etc/cron.d/`, `/etc/systemd/system/` ve bu RPC'yi koruyan `/etc/celikpanel/agent.token` de önekin içindeydi. Root olarak dosya yazmak denetim kaydina hic girmiyordu, oysa servis durdurmak giriyordu. (2) `Agent.EnableRepo` imza anahtari adresini ve apt kaynak satirini ISTEKTEN alıyordu; kurulum yolu aynı seyi hep katalogdan çözerken. Panele ulaşan bir sey kendi anahtarını sabitleyip kaynak ekleyebilir, sonraki kurulum oradan karşılanabilir ve derlenmiş paket whitelist'i çökerdi. (3) Editor once yazıp sonra doğruluyordu; doğrulama düşünce koddaki yorumun dedigi gibi ("Revert? For now just return error") bozuk dosya diskte kalıyordu — canlıda kanıtlandı: /etc/nginx/nginx.conf sıfır bayta düştü, nginx bellekten sunmayi sürdürdü ve bir sonraki reload sunucuyu susturacaktı. (4) `install.sh`, `bin/` ve `web/dist` varsa derlemeyi atlıyordu; bu dizinler git'te olmadığı için `git pull`dan sag çıkıyor, yani `update.sh` yeni kaynağı çekip ESKI binary'yi kuruyor ve "servisler yeni yapiyla yeniden başladı" diyordu. (5) Panelde gösterilen `v0.1.0` elle yazılmış bir metindi; yanındaki commit damgası yalnız on yuz paketindendi | `cmd/agent/{main.go,config_write.go,repo_rpc.go}`, `cmd/panel/{main.go,repo_handlers.go,version.go}`, `install.sh`, `Makefile`, `Layout.tsx` | ✅ Kapandı (25 Tem): `configWriteAllowed` — filepath.Clean, sembolik bağ reddi, makinenin kendi anahtar/unit/cron/apt dosyaları için açık yasak listesi ve bunun dışında yalnız TARAYICININ bir katalog bileşeni için bulduğu dosyalar + panelin kendi dizinleri; izin listesi elle yazılmadı, katalogdan türetiliyor - EnableRepo yalnız id alir, URL'i ve kaynak satirini kendi derlenmiş kataloğundan okur (kural: **agent, üzerine is yaptığı her olguyu kendi kataloğundan yeniden türetir; panel taşıyıcıdır, yetkili değil**) - yaz-dogrula-GERI AL (nginx -t, postfix check, doveconf -n, apachectl configtest; doğrulayıcısı olmayan dosya olduğu gibi yazılır) - root yazma ve retler denetim kaydında, ret 403 CONFIG_PATH_REFUSED / doğrulama hatası 422 CONFIG_INVALID ile sebebiyle görünür - git checkout HER ZAMAN yeniden derler - sürüm ve commit bağlama anında HER İKİ binary'ye gömülür, footer `/api/v1/panel/version`'dan okur ve panel/agent farklı yapıdaysa uyarır. 6 test. **Canlı kanıt (iki sunucu):** 5 devralma yolu 403 ile reddedildi ve hiçbir dosya değişmedi; bilerek bozuk nginx yapılandırması gönderildi -> dosya BIRE BIR aynı kaldı, günlükte "config rolled back", nginx -t geçiyor; sürüm ucu iki sunucuda da eşleşen panel/agent commit'i döndürüyor. Ders: **bir sınır, kendisini aşmayı deneyen bir testle sınanmadıysa sınır değildir** — ve **yaz-sonra-dogrula, geri alma yoksa yıkımdır** |

| A16 | **Yeni bir bileşen eklemek iki gizli kusuru açığa çıkardı** (Valkey eklenirken, 25 Tem): (1) **Takma ad unit'i kurulu sayılıyordu.** Arch'ın valkey paketi `/usr/lib/systemd/system/redis.service` dosyasını `valkey.service`'e sembolik bağ olarak koyuyor (paket `Provides: redis` der). Tarayıcı `systemctl list-unit-files redis.service` ile bir şey bulunca "Redis kurulu" sanıyordu; Valkey kurulur kurulmaz panel hiç kurulmamış bir Redis'i "kurulu ama ölü" gösterdi ve o bileşen kendi koltuğunu işgal etti. (2) **Koltuk kuralı yalnız arayüzde uygulanıyordu.** Boston'da satır doğru biçimde "Redis ile çakışıyor" derken aynı panelin API'sine yapılan kurulum isteği valkey-server'ı kurdu: 6379 Redis'te olduğu için bağlanamadı, systemd beş başarısız başlatmadan sonra pes etti ve bileşen "etkin ama başarısız" kaldı — koltuğun var olma sebebi olan "kurulu ve ölü" sonucunun ta kendisi. Gereksinimler için zaten geçerli olan kural ("UI da engeller; atlatılamayan uygulayıcı agent'tır") çakışma için yazılmamıştı | `cmd/agent/install_rpc.go`, `internal/core/managed_services.go` | ✅ Kapandı (25 Tem): agent artık unit'in asıl adını systemd'ye soruyor (`systemctl show -p Id`) ve bir ad başka bir bileşenin unit'ine çözülüyorsa onu kurulu saymıyor; bileşenin kendi adları arasındaki takma adlar (Debian valkey-server ↔ Arch valkey) sayılmaya devam ediyor. Karar saf bir fonksiyona ayrıldı (`unitProvesInstalled`) ki systemd olmadan test edilebilsin - `core.SeatTakenBy` koltuk kuralının tek sahibi oldu ve agent onu gereksinim denetiminin hemen yanında uyguluyor, düz Türkçe bir gerekçeyle 409 döndürerek. 9 test (takma ad 4, koltuk 5). **Canlı kanıt:** Boston'da Valkey kurulum isteği 409 ile reddedildi ve Redis'e dokunulmadı; Frankfurt'ta Valkey panelden kurulup çalıştı (PING=PONG, SET/GET, 6379 yalnız yerel) ve Redis dürüstçe "kurulu değil, Valkey ile çakışıyor" oldu. Deneme kurulumu panelden geri alındı. Ders: **bir kuralı yalnız arayüzde uygulamak, uygulamamaktır** — ve **bir bileşeni kataloğa eklemek, kuralların gerçekten tuttuğunu sınayan en ucuz testtir** |

## B. Yapısal borç (reçete — sırayla ödenir)

| # | İş | Neden | Tahmin | Durum |
|---|---|---|---|---|
| B0 | **Kanamayı durdur**: A1+A2 düzeltmeleri, ölü kod gömme | İlk müşteri kırık sayfa görmesin | 1-2 gün | ✅ 11 Tem |
| B1 | **Tek API**: v2'yi v1'e katla; kiracı kapsamı auth'tan; OpenAPI üret; ön yüz tipleri üretilen istemciden | A3+A5 sınıfı hatalar derlemede ölür; 74 ham `fetch(` tek katmana iner; "API-first" sözü gerçek olur | 3-5 gün | 🔶 Kısmi (18 Tem): A3 kiracı kapsamı + A4 parola şifreleme + rol ayrımı/admin kilidi + v2→v1 birleştirme + **hata sözleşmesi** KAPANDI. Sözleşme {error,code?,action?}: 11 kodlu ret, tek writeCodedError yazıcı; ön yüzde readApiError tek okuma yolu + ErrorBanner (action→panel-içi düğme), i18n err.* TR+EN, 13 dağınık res.text() tekleşti. Canlı: WEB_SERVER_REQUIRED/ADMIN_ONLY/AUTH_REQUIRED iki sunucuda doğru zarf+action. Kalan alt dilim: OpenAPI + üretilen istemci |
| B2 | **Route+authz tablosu**: `{yol, handler, roller}` tek veri yapısı | `main.go`'daki 72 `HandleFunc` + `middleware.go:117-141` elle listesi = unutulan satır sessiz authz deliği | 1 gün | 🔶 Fail-closed rol öne çekilip kapandı (17 Tem: okunamayan kullanıcı = geçersiz oturum, boş rolle ilerleme yok). Tablo + rol×uç matris testi açık |
| B3 | **Bilgi tek yerde**: servis kataloğu config/port/paketin TEK sahibi; scanner kataloğu okur | `managed_services.go` ↔ `service_scanner.go:93` çifti kanıtlı ıskaladı (pdns config, 10 Tem). İkinci kanıt (16 Tem, Arch): Hostinger Arch imajı devre dışı bir `named.service` ile geliyor → capabilities `dns_server: "bind"` diyor, kurulum yolculuğu "DNS kuruldu: Done" gösteriyor, ama Services sayfası 0/0 — iki tespit yolu (InstalledServiceIDs ↔ GetServices) aynı soruya farklı cevap veriyor; ayrıca "kurulu ama koşmuyor" bir DNS, zone sunamadığı hâlde Done sayılıyor | 1 gün | 🔶 Kısmi (21 Tem): **tür ayrımı** kapandı — her katalog kalemi `Kind` bildirir, `Daemonless = len(SystemNames)==0` sezgisi silindi, satır çizimi türe dallanır; 2 bekçi test. **Önbellek/katalog ayrımı** kapandı (A7): katalog tek sahiptir, önbellek yalnız gözlem saklar. **Sury** kapandı (B3c): Debian'da yan yana php8.x-fpm panelden kurulur. **Sürüm birinci sınıf** kapandı (B3b): tek sözleşme `Agent.ListServiceInstances` (php-fpm: Debian sürümlü unit'ler + Arch tek unit'ten gerçek sürüm; node: tarball ağaçları + sistem PATH dürüstçe `managed=false`), `extractVersion` switch'i ve `"default"` sentinel'i silindi (3 arka yüz + 3 ön yüz tüketicisi), sürümler satır içi çekmecede (kopya başına durum + başlat/durdur, node için boyut), **node katalogda** (`Kind=runtime`, `Requires: web-server` — bugüne dek yazısız reverse-proxy kuralı bildirime döndü), node sürüm kurmanın tek adresi çekmece (`AdminNodeInstall` silindi), capabilities `php_versions` artık agent'tan (Arch'ta boş dönüp PHP sitelerini bloke ediyordu). **Silme koruması** kapandı (B3d, 23 Tem): kaldırma öncesi tek soru — "bu giderse kim kırılır?" — canlı DB'den cevaplanır (bilerek tablo YOK: sites.php_version/runtime_version zaten gerçek; defter sorgudur, tablo değil). `RUNTIME_IN_USE` (sürüm bazında; engelleyen siteler `details` listesiyle) ve `SERVICE_HAS_DEPENDENTS` (bileşen bazında: php→PHP siteleri, nginx→dnsonly hariç tüm siteler, pdns→tüm domain'ler [eski kodsuz bekçinin kodlusu], postfix→kutular+yönlendirmeler, mariadb/pg→v2+eski tablo) 409 döner; zarf `details` alanı kazandı, ErrorBanner listeyi çizer, kaldırma modalı ret sırasında AÇIK kalır. Sürüm bazında kaldırma çekmecede (php: paket+yol arkadaşları purge; node: ağaç silme, ayrı uç) — koruma olmadan silme düğmesi hiç sunulmadı. Agent'ın bütün-servis kaldırması artık desenli unit'leri de söküyor (php-fpm kaldır → php8.x'ler kurulu-çalışır kalıyordu). Node kurulum kutusu yerine nodejs.org LTS listesi (adlandırılmış 3-4 seçenek); "sistem yorumlayıcısı" kaçağı kapandı (`RUNTIME_VERSION_REQUIRED`, eskiler dokunulana dek yaşar). Kaldırma sonrası sunucu tarafı yeniden tarama eklendi (kurulumda vardı, kaldırmada yoktu). **Scanner kataloğu okur** kapandı (24 Tem): agent tarayıcısının elle `serviceBases` listesi silindi — hem çekme glob'u hem sahiplik testi `core/scan_match.go`'dan (SystemNames + SystemNamePattern) türer; `Contains` gevşekliği yerine `UnitBelongsTo` (panel+agent aynı kural). "Çalışıyor ama durdu görünüyor" yalanının kaynağı (spamd/rspamd/netdata, üç kez) yapısal olarak bitti; 4 bekçi test. postgresql'e `^postgresql@` deseni. İki dağıtımda canlı: kurulu servisler doğru durum. Kalan: journey dürüstlüğü (kurulu-ama-koşmuyor DNS "Done" sayılmamalı) |
| B4 | **UI disiplini**: tek Button (CtrlButton/ActionIcon ölür), paylaşılan `fmtBytes` (5+ kopya), `confirm()` → temalı modal (8+ yer), tek Service tipi (`api.ts:13` / `ServiceList.tsx:8` / `Dashboard.tsx:28` üçüzü) | Tutarsızlık kullanıcıya sızıyor; kopyalar ayrı ayrı çürüyor | 2 gün | ⬜ |
| B5 | **Dürüstlük borcu**: golden-path smoke CI (build + stub ekran + kritik uçlar) | Artık 178 Go test dosyası ve önemli CI kapıları var; tarayıcı/kritik-uç ve gecikme kanıtı eksik | sürekli | 🔶 Genişledi: CI Go biçim/derleme/vet/test/race, shell/depo sözleşmeleri, kilitli web derlemesi/bağımlılık denetimi ve yeniden üretilebilir sürüm ürünlerini zorluyor. Kalan: tarayıcı render + kritik-uç smoke + <100ms ölçüm |

**Sıralama zorunlu kısıtı:** v0.3 (ilk gerçek kiracılar) B1 bitmeden BAŞLAYAMAZ.

## C. Gömülenler ve kalan kokular

- ✅ Gömüldü (11 Tem): `internal/repositories/database_repository.go` (sıfır referanslı ölü), kökteki `KONUSMA-GECMISI.md` (sohbet dökümü; tarih git'te), diskteki `ServiceList.tsx.backup`.
- ⬜ **Kural (24 Tem, Buypass olayından): dış servise bağlanan her satır, göndermeden önce o servisin CANLI endpoint'ine karşı doğrulanmalı.** Buypass Go SSL'i "EAB'siz ücretsiz alternatif" diye SSL sağlayıcı kaydına ekledim; canlı curl 404 verdi, web araştırması Buypass ACME'nin 15 Nis 2026'da kapandığını kesinleştirdi (bugün 24 Tem). Menüde ölü CA = "kurulunca çalışmayan satır" tuzağının ağ sürümü. Paket adı nasıl iki dağıtımda `apt-cache/pacman` ile doğrulanıyorsa, ACME dizini / vendor deposu / webhook URL'si gibi her dış uç da bir kez gerçek isteğe karşı sınanmalı — URL de paket gibi bayatlar, hatta ölür. B5 smoke'una aday: katalog/registry'deki dış URL'lere periyodik erişilebilirlik kontrolü.
- 🔶 Kısmen azaltıldı: barındırılan vhost'lar başlangıçta DB durumundan yeniden üretiliyor ve 4.096 satırın üstünde fail-closed duruyor. Çalışan sunucu için operatörün tetikleyebildiği reconcile eylemi ve ilerleme raporu hâlâ yok.
- ⬜ Koku: `cmd/panel` 113 dosyalık tek düz paket (35.388 ürün-kaynak satırı) — B1/B2 sırasında doğal bölünür; ayrı bir "büyük yeniden paketleme" YAPMAYIN (churn riski kazancından büyük).
- ⬜ Koku: `docs/CelikPanel Pano.html` 813KB blob — tasarım referansı; bilinçli tutuluyor, büyürse LFS/link.
- ⬜ Koku: `en.ts` 900+ anahtar tek dosya + **dizgide kesme işareti derlemeyi bozar** — katkı tuzağı; B4 sırasında en azından belgeli lint.
- ✅ Güncel düzeltme ağacında gömüldü: `cmd/debug_mariadb/main.go` silindi. Sürüm ve dağıtım kanıtı bu kaynak-ağaç durumundan ayrıdır.
- ✅ Güncel ağaçta kapandı: yer tutucu Add-ons sayfası, sınırlı katalog yönetimi olan DB destekli Mağaza kataloğuna dönüştü (`docs/STORE.tr.md`). Hak verme bilinçli olarak bileşen yaşam döngüsü işlemlerinden ayrıdır; canlı dağıtım kanıtı ayrıdır.
- ✅ Güncel ağaçta kapandı (18 Ağu): **PowerDNS ile BIND artık tek, doğrudan, yönlü ve kurtarılabilir yetkili-DNS yaşam döngüsünü paylaşır.**
  Kesin nameserver kimliği, yerel/eş adresleri, birincil/ikincil rolü ve topoloji ilk motor eyleminden önce hazırlanır; etkinleştirmeden sonra değişmez olur.
  Her rol BIND veya PowerDNS'i doğrudan etkinleştirebilir; önce PowerDNS ile bootstrap ya da geçici motor adımı yoktur.
  Sabit hedef topoloji Frankfurt/NS1'in doğrudan BIND birincil, Boston/NS2'nin PowerDNS ikincil olmasıdır.
  Boston'daki mevcut boş, panel-yönetimli tek-sunucu PowerDNS; kesin veritabanı/config/unit anlık görüntüsü, sınırlı yeniden başlatma, hedef kanıtı ve geri dönüşle yerinde incelenip yeniden yapılandırılır; BIND üzerinden geçirilmez.
  Birincilin eşten önce kurulmasına izin verilir; ancak PairReady kalıcı yerel kataloğu AXFR ile, kaynak adresi bağlanmış eş katalog AXFR'sinin birebir aynı seri ve üyeliğini, eş kataloğunun UDP ve TCP üzerinden yetkili SOA'sını ve her üyenin kalıcı beklenen SOA serisini yerelde/eşte kanıtlayana kadar eş-bekliyor kalır; panelde tüm yerel zone yazıları fail-closed engellenir.
  Silme ayrıca eşte zone AXFR'sinin bulunmadığını gerektirir; başarılı aktarım bayat kopyayı kanıtlar ve reddedilir. İkincil her zaman yerel yazılara kapalıdır.
  Yönetilen BIND varsayılan olarak `allow-transfer { none; };` kullanır; yalnız eşli BIND ikincil kesin birincil `/32` adresine izin verir, PowerDNS eş ACL'leri ise yalnız kesin eşi adlandırır.
  Motor-bağımsız katalog serisi birincil BIND↔PowerDNS geçişlerinde korunur; yalnız üyelik ekleme/silme/yeniden eklemede ilerler, yalnız kayıt değişikliğinde sabit kalır ve en büyük değerde taşmayı fail-closed reddeder.
  Seri alanı bulunmayan yayımlanmış `v0.1.0-alpha.27` kaynak receipt'i için değer yalnız birebir eşleşen kesin kalıcı ve canlı backend kanıtından türetilebilir ve yeni günlük ile receipt'e bağlanmak zorundadır.
  TSIG henüz yoktur; paylaşılan veya dinamik NAT uçları desteklenmez.
  Panel durumu ile agent'ın root-sahipli günlüğü aynı işlem kimliğini taşır: kurtarma ya kesin hedefi kanıtlayıp tamamlar ya da önceki dosyaları, veritabanını, nesil işaretçisini ve servis durumunu eksiksiz geri getirip kanıtlar; belirsizlik tahmin edilmez, kilitli kalır.
  Release ve canlı-sunucu kanıtı bu kaynak-ağaç durumundan ayrıdır.
- ⬜ Koku: agent'ta hâlâ 93 doğrudan komut çalıştırma yeri var (`tools/repo-metrics.sh`). Bayat ve kullanılmayan `AgentRPC` arayüzü silindi; fakat gerçek platform-adaptörü sınırı açık ve arayüz silmek taşınabilirliği kanıtlamıyor.

## D. Felsefe ihlalleri (anayasa vs. kod)

1. **"Dürüstlük kuralı"** (test+güvenlik+doküman yoksa bitmedi) — ağaçta artık 178 Go test dosyası ve önemli CI kapıları var; tarayıcı/kritik-uç ve gecikme kanıtı hâlâ açık. → B5.
2. **"Tek bariz yol"** — iki API sürümü, üç düğme sistemi, iki config yönetimi. → B1+B4.
3. **"API-first"** — OpenAPI yok, tipler elle üç kez. → B1.
4. **"Hız <100ms"** — tek ölçüm yok. → B5 smoke'una bir ölçüm satırı.
5. **D-009 gerilimi** (panel=DNS otoritesi): tutarlı ve savunulur, ama dış-DNS'li müşteri kitlesini kapıda çevirir — v0.3 pazara açılmadan bilinçli yeniden tartılmalı (karar değişmese bile kayıt güncellensin).

## E. Öz eleştiri (kayda geçsin)

Bu borcun bir kısmı taze ve denetimi yazan masadan çıktı (10 Tem tasarım turu):
üç düğme sisteminin ikisi, Dashboard'daki 7 ham fetch ve bir hatayı maskeleyen
sadakatsiz test stub'ı. Ders `tools/dev-preview/preview-server.py` başlığına
gömüldü: *stub gerçek şemayı tipleriyle taklit eder.*

**21 Tem — A7'yi ben ürettim ve neredeyse kapalı gözle geçiyordum.** B3'ün ilk
diliminde katalogdaki 19 kaleme `Kind` ekledim; yerel testler geçti, derleme
temizdi, iki sunucuya dağıttım. Canlıda API `kind:""` döndürdü — hepsinde.
Yerelde doğru, üründe boş. Sebep eklediğim alan değil, alanın yanından geçtiği
önbellekti.

İki ders kayda geçsin:

1. **"Testler geçti" ≠ "üründe çalışıyor".** Testlerim kataloğu doğrudan
   okuyordu; ürün yolu ise DB'den geçiyordu. Aradaki katmanı hiçbir test
   geçmiyordu. Canlı doğrulama olmasaydı bu, sessizce yanlış çizilen bir
   arayüz olarak kalırdı — operatörün "1 ay sonra yok bunu unutmuşuz"
   dediği tam bu sınıf.
2. **Kolay cevap yanlış cevaptı.** "Bir kez Tara'ya basın, düzelir" doğru ve
   yeterliydi — o an için. Ama hata `kind`'e özgü değildi: her katalog
   düzeltmesinde geri gelecekti ve her seferinde aynı cümleyle geçiştirilecekti.
   Kalıcı düzeltme, önbelleğin ne saklamaya HAKKI olduğunu sormaktı: yalnız
   gözlem. Kodda yaşayan bir gerçeği önbelleğe almak, kod ile ekranın
   birbirinden ayrı düşme biçimidir.

**Tekrar eden yöntem hatası: tek örnekle doğrulama.** Bu projede aynı şekil
şimdiye dek üç kez çıktı ve üçünde de "çalışıyor" dedikten sonra kırıldı:

| Ne tek örnekle doğrulandı | Ne görünmez kaldı |
|---|---|
| Golden path **PHP sitesiyle** kanıtlandı | Statik sitenin ACME doğrulaması hiç çalışmıyordu (A6) — ve statik dalın `.php` kaynağını sızdırdığı (A8) |
| Vendor deposu **yalnız PGDG ile** kanıtlandı | Zırhlı-anahtar varsayımı, sondaki-sayı sıralaması, "zaten kurulu" duvarı, companion ihtiyacı — dördü de PHP'de kırıldı (D-007 düzeltmesi) |
| Katalog alanları **hep var olan alanlarla** okundu | Yeni bir alan (`Kind`) eklenince önbellek onu boş yayınladı (A7) |

Ders, "daha çok test yazın" değil — daha keskin: **bir mekanizmayı tek örnekle
doğrulamak, o örneğe özgü olanı genel sanmaktır.** İkinci örnek gelene kadar
hangi varsayımın taşıyıcı olduğu bilinemez. Pratik kural: *bir soyutlama
(şablon dalı, vendor deposu, katalog alanı) ikinci somut örneğini almadan
"kanıtlandı" sayılmaz*; ve ikinci örnek eklenirken ilkine göre yazılmış her
cümle yeniden okunmalıdır — D-007'nin "zırhlı anahtar doğrudan kullanılır"
cümlesi tam olarak böyle yanlışa döndü.
