# Autopsy Report & Debt Ledger

*First audit: July 11, 2026 · Status refresh: August 2, 2026 · [Türkçe](AUTOPSY.tr.md) · The status boxes are live — whoever closes one marks it with their commit.*

This is the durable form of a brutally honest audit of the codebase: what is
broken, duplicated, dead, and where the philosophy is violated — plus the
payment plan. The goal is not blame; it is that **an engineer taking over
knows where every body is buried on day one.** Method: no assumptions, every
finding cites file:line (as of Jul 11, 2026).

## Verdict

**Refactor, NOT rewrite.** The Panel/Agent/RPC spine is sound; the golden path
is proven end-to-end (our only real asset). The problems are not architectural:
missing seams, duplicated knowledge, absent tests. A rewrite throws away proven
behavior (the Netscape mistake).

## A. Breakages (live when found)

| # | Finding | Evidence | Status |
|---|---|---|---|
| A1 | v2 DB handlers expected `TypeID==23/24`; the seed is 1=postgresql, 2=mariadb — both branches dead, the driver received an empty type | `database_v2_handlers.go` (old 280/428), seed `001_full_schema.sql:205` | ✅ Closed (Jul 11): `GetByID` now JOINs the canonical type name; handlers use `dbDriverTypeFor()` |
| A2 | Customers/resellers saw the Databases page (nav ALL) while the whole `/api/v2/` prefix is admin-gated → permanently broken page for non-admins | `nav.ts:32` ↔ `middleware.go:141` | ✅ PERMANENTLY closed (Jul 17): blanket gate removed, endpoints tenant-scoped (14 guards), server registration stays admin, nav is admin/reseller/customer; a fresh customer sees an empty list (not 404) — verified live as the customer role |
| A3 | v2 endpoints are not tenant-safe: `subscriptionID := 1 // TODO: Get from auth` | `database_v2_handlers.go:52,106,235,507` | ✅ Closed (Jul 11): 6 hardcodes removed → `callerSubscriptionID`; 6 server-scoped ops (list/create/delete × db+user) verify ownership via `canAccessDBServer` (`database_v2_authz.go`). **Admin gate STILL stands** — lifting it needs role-splitting `handleCreateDatabaseV2Server` (registering an arbitrary host/port/root-password is not a customer action); that belongs to the v0.3 tenant work |
| A4 | DB server root password stored in plain text: `// TODO: Encrypt` | `database_v2_handlers.go:138` | ✅ Closed (Jul 16): `internal/secrets` — AES-256-GCM, key at `dataDir()/secret.key` (0600, generated on first boot). Sealed on create, opened via one helper (`dbDriverFor`) at driver time; legacy plaintext rows sealed by an idempotent startup migration. Format is `enc:v1:`-prefixed — self-describing |
| A5 | `capabilities.mail_server` is BOOL while `dns_server` is a string — the type inconsistency produced a real product bug (dashboard claimed mail was installed) | `capabilities_handler.go:30` | ⬜ OPEN (frontend fixed; API consistency lands with B1) |
| A8 | **A static site served PHP source as a download**: the static vhost has no PHP handler — correct; but it had no deny rule for `.php` either. nginx finds the file, does not recognise the extension (not in `mime.types`), falls back to `default_type application/octet-stream` and makes the browser **download the source** — database password included. "PHP does not run here" must not mean "PHP source is published here" | `internal/services/templates/nginx/vhost.conf.tmpl` (static branch) | ✅ Closed (Jul 21): deny on `\.(php\|phtml\|phps\|php[0-9])$`; 403 rather than 404 on purpose (it says the file exists but is not executed — the actual diagnosis). Measured end to end against live nginx: `secret.php` 403 with nginx's error page as the body, `.well-known/acme-challenge` 200 (renewals intact), `.env` 403, missing file 404. 2 regression tests. Same family as A6 — a file in the webroot handed out as source |
| A7 | **The scan cache stored catalog fields**: `service_scan_cache` held whole API responses (name, description, icon, category, package names) → every catalog field changed in code kept its old value on screen until someone pressed Scan, and a NEWLY added catalog service never appeared at all. A panel reading the truth of its own code out of a cache | `managed_service_handlers.go` (old 62-79, 232-243) | ✅ Closed (Jul 21): the cache stores only `serviceObservation` (installed / unit up / versions / config files); the catalog is joined on every read by `catalogView` — the ONE place a response is built, so a cached read and a fresh scan cannot answer differently. The legacy format is still read (correct state without waiting for a rescan). 3 guard tests. This shipped to production as `kind:""` — see note E |
| A6 | **The vhost dotfile rule was wrong in both branches**: the PHP branch blocked only `/\.ht` → `https://site/.env` served in plain text (a Laravel/Symfony DB password, APP_KEY, mail credentials) and `.git/` was readable; the static branch blocked ALL dotfiles → `.well-known/acme-challenge` was closed, so a static site's first certificate and every renewal 403'd | `internal/services/templates/nginx/vhost.conf.tmpl` | ✅ Closed (Jul 20): both branches now use `location ~ /\.(?!well-known).*`; the regex was validated against live nginx (commit `0088c67`). The ACME break stayed invisible because the golden path was proven with a PHP site |

| A9 | **PHP version switching was broken at two layers** (caught by B3d live verification, Jul 23): (1) the FPM-as-root fix's identity-forgery gate also locked migration — the 8.3→8.4 switch returned 500 (two callers with two intents in one function); (2) past the gate, the second layer appeared: the pool moved and the DB updated while the nginx vhost kept proxying to the DELETED old socket — the site answered 502 while everything reported success. The config-drift smell (section C) made flesh | `php_pool_manager.go` MigratePool, `domain_php_handlers.go` | ✅ Closed (Jul 23): migration writes the new file directly (identity from the OLD pool — written by the panel itself; one template producer, renderPool), the gate stays; the PHP switch now regenerates the vhost from the DB row via `applySiteVhost` (write→validate→rollback→reload) and a failed regen fails the request. Regression test + the live 8.3→8.4→remove-8.3 chain verified. Lesson: **whoever installs a gate must also run the legitimate paths behind it** — 45c9b2b's live verification never tried a version switch |

| A10 | **CelikPanel's nginx integration was Debian-specific** (caught on Arch by the webmail slice, Jul 24): site vhosts use `sites-enabled/`, dbtools + webmail use `conf.d/`; Debian nginx auto-includes both, Arch's minimal `nginx.conf` includes NEITHER and the dirs do not exist. Result: on Arch every vhost/dbtool/webmail conf the panel drops is INVISIBLE — nginx says "installed" but no site, dbtool or webmail can be served. Frankfurt had only DNS-only domains so far, so site hosting was never tried and the gap stayed hidden. The operator caught it through webmail ("how can there be roundcube without nginx, nonsense") | `nginx_ready.go`, `service_install_handler.go` | ✅ Closed (Jul 24): `Agent.EnsureNginxReady` — makes an installed nginx panel-ready on any distro (conf.d + sites-available + sites-enabled dirs, missing includes into the http block, write→`nginx -t`→roll back on failure→reload); idempotent, untouched on Debian. The panel calls it after nginx install. Live proof on both distros: on Frankfurt, installing nginx VIA THE PANEL added the includes, roundcube /webmail/ 200. **Lesson (new rule): if an install reports success the component must be REALLY functional — "installed means it works" is guaranteed, a half-install is broken.** And nothing but diagnosis is done by hand over SSH; install/uninstall/configure go through the panel API (else the panel itself is not tested) |

| A11 | **Dovecot installed but did not run on Arch** (made visible by the scanner unification, Jul 24): (1) Arch's stock `dovecot.conf` references `/etc/dovecot/ssl-{cert,key}.pem` but Arch does not generate them (Debian's `dovecot-core` does) → the daemon refuses to start with "cert_file: open() failed"; (2) `ConfigureMailStack` refused outright without postfix, so a Dovecot installed alone was NEVER configured; (3) the agent starts the unit the moment the package lands (no cert yet → failed) and systemd leaves it "failed", so even a later correct configuration did not bring the service up | `mail_stack_rpc.go`, `service_install_handler.go`, `main.go` | ✅ Closed (Jul 24): `ensureDovecotSSLCert` (self-signed pair via openssl when the config asks for those files and they are missing; never touches existing ones — a real cert installed later survives) · a half stack is legitimate, whichever half is present gets configured (IMAP-only is a valid install) · `ResetFailedUnit` + `StartService` AFTER configuration. On Frankfurt a from-scratch install via the panel worked in one shot (143+993 listening, panel "active (running)"); Boston unaffected. Same lesson: **a component the panel installs must really run; the panel completes what the distro left out** (the D-018 hybrid principle) |

| A12 | **An installed spam filter was filtering nothing, and on Arch Postfix rejected EVERY incoming message** (surfaced when the operator's question — "do spamd and spamassassin work together" — turned into a live audit, 24-25 Jul): (1) on Boston, Rspamd was installed and "Running" while `smtpd_milters` was EMPTY — the daemon up, its ports listening, not one message passing through it; nothing owned the milter settings, the DKIM step wrote `smtpd_milters = <opendkim>` outright and a second filter would have silently erased it (last writer wins, no error). (2) Once the chain was wired and the first test message went through Frankfurt, the real break appeared: `hash:` was hardcoded and Arch builds postfix without Berkeley DB → all three virtual maps dead, every arriving message `451 Temporary lookup failure`. `postconf -m` lists `hash` on Arch too (type compiled in, backend missing) — the feature list lies. (3) With the map type fixed the rejection moved to the next missing index: Debian runs `newaliases` in its postinst, Arch does not → no `aliases.lmdb`, so every LOCAL recipient was refused as well | `mail_stack_rpc.go`, `dkim_signing_rpc.go`, `service_install_handler.go`, `mail_tls_rpc.go` | ✅ Closed (25 Jul): `applyMilterChain` is the SINGLE owner of the milter settings (DKIM and the spam filter COMPOSE; order: DKIM first — it signs what leaves — spam filter on ARRIVING mail only; `milter_default_action=accept` so a dead filter never eats mail) · the chain is re-composed after install AND uninstall, triggered by the SEAT (`ConflictGroup=="spam-filter"`) rather than a product list · Rspamd and SpamAssassin share that seat (they can technically be installed side by side but fill the same role: two rule sets, twice the CPU, contradicting verdicts) · new catalogue field `HelperUnits` for bridge daemons that are not the service itself (spamd scores, spamass-milter is what hands the mail over; unstarted it produced "installed, Running, zero mail filtered") · `postfixMapType` probes lmdb→hash→btree by actually running postmap, falling back to texthash · `ensureAliasDatabase` plus `postconf -x` (Arch reports `alias_database = $alias_maps` unexpanded) · the SNI map discovers its type too and errors instead of going quiet if only texthash is available · a one-shot repair at panel startup, so the upgrade itself fixes existing servers. **End-to-end live proof on both distros, everything installed FROM THE PANEL:** a GTUBE test message → `554 5.7.1 Gtube pattern`, postfix `milter-reject: END-OF-MESSAGE`, rspamd `Messages scanned 0→1, action reject 100%`. Lesson: **"installed means working" does not end at the component — it must be wired INTO the stack; and paths, types and sockets are DISCOVERED, never assumed** (A10's nginx includes, A11's Dovecot certificate and this one share one root: assuming a distro's packaging choice) |

| A13 | **Manage was a dead end on most components; failures were never recorded; Netdata could not be installed on Debian** (operator, 25 Jul: "birçok servisin manage'i doğru düzgün çalışmıyor. dinamik manage sistemi yok sanırım... net data kurulamadı. ayrıca log yok"): (1) `App.tsx` held a hand-written switch of nine pages, yet the Manage button was drawn for EVERY component with `kind !== 'tool'` — Rspamd, ClamAV, Redis, Memcached and Node all landed on a "no dedicated page" message. The UI edition of the second-hand-written-list smell. (2) The audit log recorded only SUCCESSES: the netdata attempt left no trace at all, and start/stop/restart were never logged — the ledger was a highlight reel, not a record, and the operator could not show what they had done. (3) Debian/Ubuntu package netdata nowhere (`apt-cache policy netdata` → `Candidate: (none)`); the catalogue carried only a `pacman` entry, so the install died late inside apt | `App.tsx`, `ComponentDetail.tsx`, `main.go`, `service_install_handler.go`, `managed_services.go` | ✅ Closed (25 Jul): `ComponentDetail` — a DERIVED generic page drawing whatever the catalogue and the scan already know (kind, systemd unit [not always the id], installed versions, packages, open ports, config files → the same editor) plus the one thing the panel could never show: the unit's own journal via `Agent.ServiceJournal` (filter+refresh, read-only, unit name validated). The specialised pages stay because they do more than describe; the generic one needs no entry in any list · failed installs/uninstalls are audited WITH THEIR REASON, and every start/stop/restart is audited · Netdata gained the vendor's official repository (a FLAT repo: the distribution is `{codename}/` with no component list — the usual `… trixie main` line would 404) and a new `Repo.Required` field so the repo is presented as a REQUIRED step, not an optional upgrade · the response gained `ports`, and a component with none says "local only". **Live proof:** on Frankfurt the rspamd journal was read through the panel, a crafted unit name was refused with 400, and three audit rows (failed stop / successful restart / failed install with reason) were confirmed; on Boston the repo was probed with a fully isolated apt run that touched no system file: `Candidate: (none)` → **2.10.4**. Lesson: **a button shown on every row must work on every row — otherwise the hand-kept list lies on screen**; and **a ledger that records only successes is not a ledger** |

| A14 | **WireGuard did not work after install: the package landed, the server configuration was never created** (operator, 25 Jul: "WireGuard VPN server çalışmıyor"): installing from Components stopped at the package — no server key, no wg0.conf; the play button died with `wg-quick: /etc/wireguard/wg0.conf does not exist`. The endpoint that completes setup (`/api/v1/vpn/setup`) existed but was wired only to the VPN page's button; anyone installing from Components never saw it. Same family as A10 (nginx includes) and A11 (Dovecot TLS): the half-install | `service_install_handler.go`, `App.tsx` | ✅ Closed (25 Jul): install hook — after WireGuard installs, the panel calls `Agent.SetupVPN` itself (key + wg0.conf + NAT + IP forwarding + enable/start; idempotent, syncs ledger peers), and `syncFirewall` in the install flow already opens 51820/udp. The row's Manage now goes to the real VPN page. **Live proof:** on Frankfurt a one-step install from Components → immediately `configured:true, running:true`, port listening, nft rule present; a peer was issued (`wg show` shows 1 peer). Boston's half-install was completed through the panel API's setup → `running:true`. Same lesson: **an install is not done until the component is wired into the stack** |

| A15 | **The agent trusted request-supplied paths and URLs; the config editor could destroy the file it edited** (surfaced while measuring the extension architecture, 25 Jul): (1) `Agent.UpdateConfig`'s only check was "does the path start with `/etc/`", with no cleaning — `/etc/../root/.ssh/authorized_keys` PASSED and landed in /root/.ssh; `/etc/cron.d/`, `/etc/systemd/system/` and `/etc/celikpanel/agent.token` (the credential guarding this very RPC) were inside the prefix by construction. Writing a root-owned file was never audited, while stopping a service was. (2) `Agent.EnableRepo` took the signing-key URL and the apt source line FROM THE REQUEST, while the install path resolved the same kind of fact from the catalogue. Anything reaching the panel could pin its own key and add a source, the next install could be satisfied from it, and the compiled package whitelist would collapse. (3) The editor wrote first and validated after; on failure the broken file stayed on disk, exactly as the code's own comment admitted ("Revert? For now just return error") — proven live: /etc/nginx/nginx.conf went to zero bytes, nginx kept serving from memory, and the next reload would have silenced the server. (4) `install.sh` skipped the build when `bin/` and `web/dist` existed; both are gitignored and survive `git pull`, so `update.sh` pulled new source, installed the OLD binary, and printed "services restarted with the new build". (5) The `v0.1.0` in the UI was a hand-typed literal and the commit stamp came from the frontend bundle only | `cmd/agent/{main.go,config_write.go,repo_rpc.go}`, `cmd/panel/{main.go,repo_handlers.go,version.go}`, `install.sh`, `Makefile`, `Layout.tsx` | ✅ Closed (25 Jul): `configWriteAllowed` — filepath.Clean, symlink refusal, an explicit deny-list for the machine's own keys/units/cron/apt files, and otherwise only files the SCANNER discovered for a catalogue component plus the panel's own directories; the allow-list is derived from the catalogue, not hand-written - EnableRepo takes only an id and reads the URL and source line from its own compiled catalogue (rule: **the agent re-derives every fact it acts on from its own catalogue; the panel is a courier, not an authority**) - write-validate-ROLL BACK (nginx -t, postfix check, doveconf -n, apachectl configtest; a file with no known checker is written as-is) - root writes and refusals are audited, and refusals surface as 403 CONFIG_PATH_REFUSED / 422 CONFIG_INVALID with the real reason - a git checkout ALWAYS rebuilds - version and commit are linked into BOTH binaries, the footer reads `/api/v1/panel/version`, and a panel/agent build mismatch is warned about. 6 tests. **Live proof (both servers):** five takeover paths refused with 403 and not one file changed; a deliberately broken nginx config was submitted -> the file stayed BYTE-IDENTICAL, the log said "config rolled back", nginx -t passes; the version endpoint returns matching panel/agent commits on both hosts. Lesson: **a boundary that has not been tested by something trying to cross it is not a boundary** — and **write-then-validate without a rollback is destruction** |

| A16 | **Adding one new component exposed two hidden defects** (while adding Valkey, 25 Jul): (1) **An alias unit counted as installed.** Arch's valkey package ships `/usr/lib/systemd/system/redis.service` as a symlink to `valkey.service` (the package declares `Provides: redis`). The scanner found something with `systemctl list-unit-files redis.service` and read it as "Redis is installed"; the moment Valkey was installed the panel showed a Redis that had never been installed as "installed but dead", and that phantom occupied its own seat. (2) **The seat was enforced in the UI only.** On Boston the row correctly said "conflicts with Redis", yet an install request to the same panel's API installed valkey-server: with Redis holding 6379 it could not bind, systemd gave up after five failed starts, and the component sat there "enabled but failed" — precisely the "installed and dead" outcome the seat exists to prevent. The rule already true of requirements ("the UI blocks this too; the agent is the enforcement that cannot be skipped") had never been written for conflicts | `cmd/agent/install_rpc.go`, `internal/core/managed_services.go` | ✅ Closed (25 Jul): the agent now asks systemd for a unit's canonical name (`systemctl show -p Id`) and refuses to count a name that resolves to another component's unit; aliases between a component's OWN names (Debian valkey-server ↔ Arch valkey) still count. The decision was split into a pure function (`unitProvesInstalled`) so it is testable without systemd - `core.SeatTakenBy` became the single owner of the seat rule and the agent enforces it right beside the requirements check, returning 409 with a plain-language reason. 9 tests (4 alias, 5 seat). **Live proof:** on Boston the Valkey install was refused with 409 and Redis was untouched; on Frankfurt Valkey installed from the panel and worked (PING=PONG, SET/GET, 6379 loopback only) while Redis honestly read "not installed, conflicts with Valkey". The trial install was undone through the panel. Lesson: **enforcing a rule only in the UI is not enforcing it** — and **adding a component to the catalogue is the cheapest test of whether the rules actually hold** |

## B. Structural debt (the prescription — paid in order)

| # | Work | Why | Estimate | Status |
|---|---|---|---|---|
| B0 | **Stop the bleeding**: A1+A2 fixes, bury dead code | The first customer must not meet a broken page | 1-2 days | ✅ Jul 11 |
| B1 | **One API**: fold v2 into v1; tenant scope from auth; generate OpenAPI; frontend types from the generated client | Kills the A3+A5 class at compile time; collapses 74 raw `fetch(` calls into one layer; makes "API-first" true | 3-5 days | 🔶 Partial (Jul 18): A3 tenant scoping + A4 password encryption + role split/admin gate + v2→v1 merge + **the error contract** DONE. Contract {error,code?,action?}: 11 coded refusals, one writeCodedError writer; frontend readApiError as the one reader + ErrorBanner (action→in-panel button), i18n err.* TR+EN, 13 scattered res.text() unified. Live: WEB_SERVER_REQUIRED/ADMIN_ONLY/AUTH_REQUIRED with the right envelope+action on both servers. Remaining sub-slice: OpenAPI + generated client |
| B2 | **Route+authz table**: one `{path, handler, roles}` structure | 72 hand `HandleFunc` lines in `main.go` + the hand list at `middleware.go:117-141` = a forgotten line is a silent authz hole | 1 day | 🔶 Fail-closed roles pulled forward and closed (Jul 17: unreadable user = invalid session, no proceeding with an empty role). The table + role×endpoint matrix test remain open |
| B3 | **Knowledge in one place**: the service catalog owns config paths/ports/packages; the scanner reads the catalog | The `managed_services.go` ↔ `service_scanner.go:93` duplication provably missed (pdns config, Jul 10). Second proof (Jul 16, Arch): Hostinger's Arch image ships a disabled `named.service` → capabilities says `dns_server: "bind"`, the setup journey shows "Install a DNS server: Done", yet the Services page says 0/0 — two detection paths (InstalledServiceIDs ↔ GetServices) answer the same question differently; and an installed-but-stopped DNS counts as Done although it serves no zone | 1 day | 🔶 Partial (Jul 21): **kind separation** closed — every catalog entry declares its `Kind`, the `Daemonless = len(SystemNames)==0` heuristic is deleted, row rendering branches on kind; 2 guard tests. **Cache/catalog split** closed (A7): the catalog is the single owner, the cache stores observations only. **Sury** closed (B3c): side-by-side php8.x-fpm installs from the panel on Debian. **Version first-class** closed (B3b): one contract `Agent.ListServiceInstances` (php-fpm: Debian versioned units + the real version out of Arch's single unit; node: tarball trees + system PATH honestly `managed=false`), the `extractVersion` switch and the `"default"` sentinel deleted (3 backend + 3 frontend consumers), versions live in an in-row drawer (per-copy status + start/stop, size for node), **node in the catalog** (`Kind=runtime`, `Requires: web-server` — the until-now unwritten reverse-proxy rule made declarative), the drawer is the single address for installing node versions (`AdminNodeInstall` deleted), capabilities `php_versions` now from the agent (it returned empty on Arch and blocked PHP sites). **Deletion protection** closed (B3d, Jul 23): before anything is removed, one question — "who breaks if this goes?" — answered from the live DB (deliberately NO table: sites.php_version/runtime_version already hold the truth; the ledger is a query). `RUNTIME_IN_USE` (per version; blocking sites in `details`) and `SERVICE_HAS_DEPENDENTS` (per component: php→PHP sites, nginx→all non-dnsonly sites, pdns→all domains [the old uncoded guard, now coded], postfix→mailboxes+forwardings, mariadb/pg→v2+legacy tables) return 409; the envelope gained `details`, ErrorBanner renders the list, the uninstall dialog stays OPEN through a refusal. Per-version removal lives in the drawer (php: package+companions purged; node: tree removal, own endpoint) — no delete button ever shipped unguarded. The agent's whole-service uninstall now also strips pattern-matched units (uninstalling php-fpm used to leave php8.x installed and running). The node install box became the nodejs.org LTS list (3-4 named options); the "system interpreter" escape is closed (`RUNTIME_VERSION_REQUIRED`; legacy rows live until touched). Server-side rescan added after uninstall (install had it, uninstall didn't). **Scanner reads the catalog** closed (Jul 24): the agent scanner's hand-written `serviceBases` list is deleted — both the fetch glob and the ownership test derive from `core/scan_match.go` (SystemNames + SystemNamePattern); `UnitBelongsTo` replaces the loose `Contains` (panel+agent, one rule). The source of the "running but shown stopped" lie (spamd/rspamd/netdata, three times) is structurally gone; 4 guard tests. postgresql got a `^postgresql@` pattern. Live on both distros: installed services report the right status. Remaining: journey honesty (an installed-but-stopped DNS must not count as "Done") |
| B4 | **UI discipline**: one Button (CtrlButton/ActionIcon die), shared `fmtBytes` (5+ copies), `confirm()` → themed modal (8+ sites), one Service type (the `api.ts:13` / `ServiceList.tsx:8` / `Dashboard.tsx:28` triplets) | Inconsistency leaks to users; copies rot independently | 2 days | ⬜ |
| B5 | **The honesty debt**: golden-path smoke CI (build + stub screenshots + critical endpoints) | 178 Go test files now exist and CI has substantial gates; browser/critical-endpoint and latency proof remain incomplete | continuous | 🔶 Expanded: CI enforces Go format/build/vet/test/race, shell/repository contracts, locked web build/dependency audit, and reproducible release artifacts. Remaining: browser render + critical-endpoint smoke + <100ms measurement |

**Hard ordering constraint:** v0.3 (first real tenants) MUST NOT start before B1 is done.

## C. Buried, and remaining smells

- ✅ Buried (Jul 11): `internal/repositories/database_repository.go` (zero-reference dead code), root-level `KONUSMA-GECMISI.md` (chat dump; history lives in git), on-disk `ServiceList.tsx.backup`.
- ⬜ **Rule (Jul 24, from the Buypass incident): every row that points at an external service must be verified against that service's LIVE endpoint before shipping.** I added Buypass Go SSL to the SSL-provider registry as "the free, EAB-less alternative"; a live curl returned 404, and web research confirmed Buypass's ACME service shut down on 15 Apr 2026 (today is Jul 24). A dead CA in a dropdown is the network version of the "installs but does not work" trap. Just as a package name is verified on both distros with `apt-cache`/`pacman`, every external endpoint — ACME directory, vendor repo, webhook URL — must be tested against a real request once; a URL goes stale, even dies, exactly like a package. Candidate for the B5 smoke: periodic reachability checks on external URLs in the catalog/registry.
- 🔶 Partially mitigated: hosted vhosts are regenerated from DB state at startup, fail-closed above 4,096 rows. A running server still has no explicit operator-triggered reconcile action or progress report.
- ⬜ Smell: `cmd/panel` is one flat 113-file package (35,388 product-source lines) — it splits naturally during B1/B2; do NOT run a separate "great repackaging" (churn outweighs gain).
- ⬜ Smell: `docs/CelikPanel Pano.html`, an 813KB blob — kept deliberately as design reference; move to LFS/link if it grows.
- ⬜ Smell: `en.ts` with 900+ keys in one file, and **an apostrophe in a string breaks the build** — a contributor trap; at minimum a documented lint during B4.
- ✅ Buried in the current remediation tree: `cmd/debug_mariadb/main.go` is deleted. Release and deployment proof remain separate from this source-tree status.
- ✅ Closed in the current tree: the placeholder Add-ons page became the DB-backed Store catalogue with bounded catalog administration (`docs/STORE.md`). Entitlement granting remains deliberately separate from component lifecycle operations; live deployment proof is separate.
- ✅ Closed in the current tree (Aug 18): **PowerDNS and BIND now share one direct, directional and recoverable authoritative-DNS lifecycle.**
  Exact nameserver identity, local/peer addresses, primary/secondary role and topology are staged before the first engine action and become immutable after activation.
  Either role may activate BIND or PowerDNS directly; there is no PowerDNS-first bootstrap or temporary-engine hop.
  The frozen target topology is Frankfurt/NS1 as direct BIND primary and Boston/NS2 as PowerDNS secondary.
  Boston's existing empty, panel-managed standalone PowerDNS is reviewed and reconfigured in place with an exact database/config/unit snapshot, bounded restart, target proof and rollback; it is not switched through BIND.
  Installing the primary before the peer is allowed, but it remains pair-pending and all panel-local zone writes fail closed until PairReady proves the durable local catalog by AXFR, a source-bound peer catalog AXFR with the exact same serial and membership, the peer catalog's authoritative SOA over UDP and TCP, and every member's durable expected SOA serial locally and on the peer.
  A deletion also requires the peer zone AXFR to be absent; a successful transfer proves a stale copy and is rejected. A secondary is always locally read-only.
  Managed BIND defaults to `allow-transfer { none; };`; only a paired BIND secondary allows its exact primary `/32`, while PowerDNS peer ACLs name only the exact peer.
  The engine-neutral catalog serial survives primary BIND↔PowerDNS switches, advances only for membership add/delete/re-add, stays fixed for record-only changes and fails closed at maximum overflow.
  For a released `v0.1.0-alpha.27` source receipt that lacks the serial, the value may be derived only from exact matching durable and live backend evidence and must be bound into the new journal and receipt.
  TSIG is not implemented, and shared or dynamic NAT endpoints are unsupported.
  Panel state and the agent's root-owned journal carry the same operation identity: recovery either proves and commits the exact target or restores and proves the complete prior files, database, generation pointer and service state; ambiguity remains locked instead of being guessed.
  Release and live-server proof remain separate from this source-tree status.
- ⬜ Smell: the agent still has 93 direct command-execution sites (`tools/repo-metrics.sh`). The stale unused `AgentRPC` interface is gone, but a real platform-adapter boundary remains open; deleting an interface does not prove portability.

## D. Philosophy violations (constitution vs. code)

1. **"The honesty rule"** (no tests+security+docs = not done) — the tree now has 178 Go test files and substantial CI gates, but browser/critical-endpoint and latency proof remain open. → B5.
2. **"One obvious way"** — two API versions, three button systems, two config-management styles. → B1+B4.
3. **"API-first"** — no OpenAPI; types hand-written three times. → B1.
4. **"Speed <100ms"** — not a single measurement exists. → add one measurement line to the B5 smoke.
5. **The D-009 tension** (panel = DNS authority): coherent and defensible, but it turns away the entire external-DNS customer base — re-weigh deliberately before v0.3 goes to market (even if the decision stands, refresh the record).

## E. Self-criticism (for the record)

Part of this debt is fresh and came from the desk that wrote this audit
(the Jul 10 design round): two of the three button systems, the 7 raw fetches
in Dashboard, and an unfaithful test stub that masked a real bug. The lesson is
baked into the header of `tools/dev-preview/preview-server.py`: *the stub
mimics the real schema, types included.*

**Jul 21 — I created A7 and nearly walked past it with my eyes shut.** In B3's
first slice I added `Kind` to all 19 catalog entries; local tests passed, the
build was clean, I deployed to both servers. Live, the API returned `kind:""` —
for every one of them. Right locally, empty in production. The cause was not
the field I added but the cache it happened to pass through.

Two lessons for the record:

1. **"Tests pass" ≠ "works in production".** My tests read the catalog
   directly; the production path went through the DB. No test crossed the
   layer in between. Without live verification this would have stayed a
   silently mis-drawn UI — exactly the class the operator means by "a month
   later, oh, we forgot this one".
2. **The easy answer was the wrong answer.** "Press Scan once and it fixes
   itself" was true and sufficient — for that moment. But the bug was not
   specific to `kind`: it would return on every catalog edit, and be waved
   away with the same sentence each time. The durable fix was to ask what the
   cache has a RIGHT to store: observations only. Caching a fact that lives in
   code is how code and screen drift apart.

**The recurring methodological error: validating against a single example.** The
same shape has now appeared three times, and each time it broke *after* we said
it worked:

| What was validated against one example | What stayed invisible |
|---|---|
| The golden path was proven with a **PHP site** | Static sites could never pass ACME validation (A6) — and the static branch leaked `.php` source (A8) |
| Vendor repos were proven with **PGDG only** | The armoured-key assumption, trailing-integer ordering, the "already installed" wall, and the need for companions — all four broke on PHP (D-007 correction) |
| Catalog fields were read with **fields that always existed** | Adding a new one (`Kind`) made the cache publish it empty (A7) |

The lesson is not "write more tests" — it is sharper: **validating a mechanism
against one example mistakes what is specific to that example for what is
general.** Until the second example arrives, you cannot know which assumption was
load-bearing. Working rule: *an abstraction (a template branch, a vendor repo, a
catalog field) is not "proven" until it has a second concrete instance*; and when
that second instance lands, every sentence written for the first must be re-read
— D-007's "the armoured key is used directly" became false in exactly that way.
