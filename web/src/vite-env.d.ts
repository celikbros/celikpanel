/// <reference types="vite/client" />

// Injected by vite.config.ts at build time (short git commit). Shown in the
// sidebar footer so a hard-refresh reveals exactly which build is live.
// vite.config.ts tarafından build anında enjekte edilir (kısa git commit).
// Kenar çubuğu alt bilgisinde gösterilir; sert yenileme hangi build'in canlı
// olduğunu ortaya çıkarır.
declare const __BUILD__: string
