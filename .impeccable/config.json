{
  "detector": {
    "designSystem": {
      "enabled": true
    },
    "ignoreRules": [],
    "ignoreFiles": [],
    "ignoreValues": [
      {
        "rule": "border-accent-on-rounded",
        "value": "*",
        "files": [
          "web/src/components/ui.tsx",
          "web/src/i18n/index.tsx"
        ],
        "createdAt": "2026-09-06T18:30:00.000Z",
        "reason": "Claude triage: a one-sided border on a rounded-full element is the spin arc itself, not an accent border on a card. R-064 collapsed twenty-seven hand-written copies into one shared Spinner, so there are exactly two spinners left in the product and both are here: the shared one in ui.tsx, and the boot one in i18n/index.tsx, which cannot use the shared component because it renders before I18nProvider has a value and the shared Spinner reads its accessible name from useI18n, which throws outside the provider. Replaces four file-scoped entries that existed only because the spinner was copied; a test now refuses a third."
      },
      {
        "rule": "overused-font",
        "value": "open sans",
        "files": [
          "web/src/index.css"
        ],
        "createdAt": "2026-09-06T12:10:00.000Z",
        "reason": "Claude triage, sanctioned exception: the two Open Sans @font-face rules are the cPanel skin, where matching that product's face is the point, and they are split into latin / latin-ext subsets precisely so Turkish characters render without pulling the whole face. Self-hosted so the panel renders offline. Scoped to index.css: a new overused face anywhere else still reports. Inter is deliberately NOT ignored - that one is a live question about the panel's own face, not a false positive."
      },
      {
        "rule": "design-system-font-size",
        "value": "20px",
        "createdAt": "2026-09-08T04:21:05.773Z"
      }
    ]
  }
}
